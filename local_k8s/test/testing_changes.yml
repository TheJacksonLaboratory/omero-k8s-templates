apiVersion: apps/v1
kind: Deployment
metadata:
  name: omero-web
spec:
  template:
    spec:
      containers:
        - name: nginx
          image: nginx:1.21.4
          volumeMounts:
            - $patch: delete
              name: nginx-ssl
      volumes:
        - $patch: delete
          name: nginx-ssl